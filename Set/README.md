1.set hữu ích trong các thao tác :
-Loại bỏ trùng
-Kiểm tra tồn tại
-Là phần tử Unique
...

2.Thao tác set
-Tạo set
-add():Thêm phần tử vào
-has():Kiểm tra tồn tại
-delete():Xóa phần tử
-clear():Xóa toàn bộ
-size():Số lượng set
-Duyệt set: for..of
-set->Array: set->Array.from()->Array